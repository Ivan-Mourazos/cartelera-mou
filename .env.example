# Optional development-only TMDb Read Access Token.
# Do not prefix this variable with VITE_: Vite would expose it to the WebView.
TMDB_READ_ACCESS_TOKEN=

# Optional absolute path used only while developing.
FFPROBE_PATH=
